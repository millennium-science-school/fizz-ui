{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm -C playground build",
  "outputDirectory": "playground/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
